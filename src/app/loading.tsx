export default function Loading() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[var(--surface)] p-6">
      <p className="text-sm text-[var(--muted)]">Loading EduSphere…</p>
    </main>
  );
}
